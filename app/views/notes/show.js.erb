var todo = document.getElementById("todo");
var note = $(todo).find("#<%= escape_javascript("#{@note.id}")%>")
note.replaceWith("<%= escape_javascript(render 'note')%>");
var title = document.getElementById("form_title");
var value = title.value;
title.focus();
title.value = "";
title.value = value;
console.log(<%=@note.id%>);
