<%= tag("div", {id: "#{note.id}", class: "note #{note.color}_note drag-drop", data: { draggable: true } }, true) %>
    <h4><%= note.title %></h4>
    <%=link_to "x", note, method: :delete, remote: true, class:"btn delete_button"%>
</div>