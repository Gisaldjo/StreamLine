$('#calendar_body').fullCalendar('removeEvents', [<%= @task.id %>]);
$('#calendar_body').fullCalendar(
  'renderEvent', 
  $.parseJSON("<%=j render(@task, format: :json).html_safe %>"), 
  true
);
$('.modal').modal('hide');